# ORACLE RETIRED — 2026-07-03

The capture was verified complete against this code (3-round adversarial loop +
original-design census + blind audit) and SEALED by the user. Per the Build
Protocol, **builders never read this folder again** — the sealed guide
(`src/guide.jsx`) is the sole source for the rebuild. `scripts/guard.mjs`
fails the build on any import/reference from app code. Git history preserves
everything here if forensics are ever needed.
